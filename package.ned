//
// Package for the Food Delivery Network simulation (INET-based).
//
// @namespace maps this NED package to the C++ namespace "fooddelivery",
// so the simple modules declared here resolve to the matching C++ classes
// (e.g. NED type "fooddelivery.CustomerApp" -> C++ class fooddelivery::CustomerApp).
//
package fooddelivery;

@namespace(fooddelivery);
